# Project Memory — Rafli Life Tracker

> Complements `CLAUDE.md` (rules), `docs/PRD.md`/`docs/SRS.md` (spec) with things those docs don't capture: recent decisions, why they were made, working preferences learned through collaboration, and loose ends still open. Upload this alongside the rest of `docs/` as Project Knowledge. Update it after any session that changes product behavior, not just when asked — stale memory is worse than no memory.

---

## Snapshot

- **Product**: Rafli Life Tracker — personal life OS (Career, Finance, Goals, Skills, Life Compass). Single user (Rafli Akbar), Phase 1 released.
- **Stack**: Next.js 14 (App Router) + React 18 + Tailwind 3, Zustand + localStorage synced to Supabase (whole-state blob, see `docs/features/supabase-sync.md`), Framer Motion, Recharts, FastAPI health-check stub.
- **Nav** (current, as of 2026-07-18): Dashboard, Goals, Career, Finance, Skills, Life Compass, Asisten AI.
- **AI**: OpenRouter free-tier-only chat assistant at `/ai`, read-only. Dashboard/Life Compass insights are rule-based selectors, never LLM calls.

## How to work with Rafli (learned, not written elsewhere)

- **Move fast, don't stall on routine approval.** Rafli explicitly said to assume approval by default rather than asking. Exception: when a request conflicts with a standing `CLAUDE.md` rule (e.g. "don't change nav structure") — name the conflict and proposed resolution explicitly, get a real answer for *that*, but don't add approval checkpoints beyond it.
- **Ponytail (lazy-code) mode is the default posture** for implementation work in this repo — smallest correct diff, reuse before adding, no speculative abstractions.
- **Prefer real data over placeholders when it exists.** The Career Journey redesign replaced 8 fictional seed milestones with 13 parsed from Rafli's actual resume — that's the expected default, not a one-off.
- **Verification is part of "done," not optional.** Production build + a Playwright smoke pass (click through the actual flow, screenshot desktop/mobile 390px/dark mode, check console errors) before calling a UI change complete.
- **Never commit automatically.** Draft the commit message, stage nothing without being asked. Rafli has committed doc-sync work himself before (see log below) — don't assume silence means "go ahead and push."
- **Docs are a living deliverable.** After a feature-level change, `CLAUDE.md`/`PRD.md`/`SRS.md` need updating in the same pass — Rafli caught this being skipped once already (see 2026-07-18 entry below) and had to ask for it explicitly. Don't wait to be asked next time.

## Recent work log (reverse chronological, dated)

### 2026-07-20 — AI chat assistant gets suggest-only write capability
Rafli asked directly, right after the Goals progress-source work: "menarik jika Asisten AI bisa mengubah data saya sesuai approval saya... jangan read only, tapi update dan create juga... mesti ada approval saya dengan plan yang ia mau ubah seperti apa." This is a deliberate reversal of a standing `CLAUDE.md` rule ("Chat assistant... Read-only, no write capability") — but that rule's own text already carved out exactly this exception ("unless explicitly asked to write directly"), so this is the rule working as designed, not a rule being broken. Confirmed scope via `AskUserQuestion` before building: (1) every entity except Reflection/Letter/Weekly Review is writable — those stay off-limits because they're the user's own words, not something an AI should draft; (2) create + update only, no delete/archive of anything, ever.
**Design reused, not invented**: the exact "AI proposes → user reviews a checklist → approve calls a normal store action" pattern already existed twice (Goals/Skills `ActionPlanPanel`, Finance's `FinancialPlanCard`) — extended that same shape to chat instead of building a new one. Kept the normal Q&A path (streamed SSE) completely untouched; write-looking messages (cheap keyword heuristic, `looksLikeActionRequest()`) route to a *separate* buffered endpoint instead — mixing structured JSON parsing into a live token stream would have been much more complexity for no real benefit, and a heuristic false positive only costs one extra round-trip (the model just replies normally with `actions: []` if it decides there's nothing to propose).
**The real design decision was id-targeting for updates**: `updateGoal`/`updateSkill`/etc. need to know *which* existing record to touch. Rather than fuzzy-matching a title client-side (error-prone, more code), the model is given a lightweight id+label list (`buildActionableEntities()` — deliberately not the full `buildContext()` Q&A payload, which is aggregated and id-less by design) and instructed to always echo a real id back, never invent one — then the server independently re-validates that id exists in the same context before accepting the action, so a hallucinated id can't slip through even if the prompt instruction is ignored.
**No delete anywhere the code could reach it**: rather than filtering out delete/archive proposals after the fact, the action whitelist (`ACTION_DEFS`) simply has no `remove*`/`archive*` entries at all — structurally impossible to execute one, not just discouraged.
**Two real bugs caught by testing against the live dev server, not by inspection**: (1) `setWeeklyBudget`'s normalizer stored `week` as a bare number while every real caller (`WeeklyLimitEditor`, `budgetWeeklyBreakdown`) keys weeks as strings `"W1"`.."W4" — an AI-approved limit wrote successfully but was an orphaned row invisible on `/finance`, only caught by reading the actual persisted `budgets` array directly rather than trusting the UI said "Diterapkan." (2) relative dates ("due besok") resolved to nonsense (2024-12-20) because the model was never told today's actual date — fixed by injecting it into the prompt. Both fixed before wrapping up; the orphaned test row was manually removed from both localStorage and Supabase since it had already round-tripped through the live sync (same test-data-hygiene caution as the Goal Progress Sources entry above).
Full detail: `docs/features/ai-assistant.md` ("Write capability" section).

### 2026-07-20 — Goal progress sources ("where does this % come from?")
Rafli asked a direct question after looking at the live Goals page: "Meningkatkan English profesional" showed 35% and he hadn't entered anything — where did the number come from? Root cause: every seed goal had a hand-typed placeholder (`progress: 35`, `metric.current: 8_400_000`, etc.) left over from the original seed data, with no visible indication it wasn't real. Fix, agreed via his own follow-up message (combination of "zero everything out" + "let progress come from Finance or Skills, settable per-goal"): (1) zeroed every fake seed number; (2) added a `progressSource` field (`manual` / `finance` + category / `skill` + skillId) with an "Atur target & sumber progress" panel on each goal in the drawer, so the user picks per-goal where the number comes from instead of it being implicit. `careerReadiness()`'s network/LinkedIn/job-search sub-goals now read through the same resolver (`goalProgressValue()`) instead of the raw `.progress` field, so a goal linked to Finance/Skills correctly feeds the Career Readiness composite too — that would've silently stayed stuck at 0% in the composite otherwise even after linking.
**Migration matters here, not just seed.js**: this app syncs to a real Supabase project (single hardcoded user), so a seed.js fix alone doesn't touch data already sitting in the user's live state. Added a one-time backfill in both `hydrate()` paths (local + Supabase-reconciled), gated on `progressSource` being absent — a goal is only ever auto-zeroed once, so it's safe to run against production data without risk of clobbering real progress the user sets going forward.
**Process note — testing against live Supabase has a real cost**: this dev server points at the same Supabase project the production Vercel deployment reads from (confirmed via `.env.local`, no separate test project exists). Playwright verification runs (linking a skill, editing a manual number, linking a finance category) briefly wrote that test data into production. Caught before wrapping up — reset the three touched goals back to `{type: "manual"}` / zero via the same UI, then re-verified clean state. Worth remembering unprompted next time this dev server is used for interactive testing, not just recalled after the fact.
Full detail: `docs/features/goal-progress-sources.md`.

### 2026-07-20 — Finance corrections + Life Compass rework (7-point brief)
Rafli sent a screenshot of the live Finance page plus a 7-point brief covering both real bugs and new features, requesting one integrated fix. Notable findings during triage: (a) point 1 ("add Tabungan/Dana Darurat as a transaction type") was solved at the UI level — auto-locking category when those Tipe options are picked — rather than migrating the `type` field to a 4-way enum, which would have touched every `type === "expense"` check in the codebase for no behavioral gain. (b) Point 4's "phantom 8.4 juta" was the `financeTargets.savings.baseline` seeded the prior session — removed entirely, `fundCurrent()` is now a pure transaction sum. (c) Point 4 also asked for staged/progressive milestones ("show 10jt first, then unlock 20jt") — `savingsProgress()`'s `target`/`pct` now reference the *next unmet milestone*, not the grand total; caught a real bug while implementing this: the Tabungan card's edit-target field would have let editing the displayed (staged) Rp10jt number silently overwrite the real Rp100jt goal — fixed by separating `target` (staged, drives the progress bar) from `editTarget` (always the grand total). (d) Point 6a's "Life Compass section won't open" turned out to be a genuine, specific bug: `WeeklyRitual.jsx`'s ritual-history rows rendered a chevron implying they were clickable but had no `onClick` at all, and only ever showed 2 of 7 submitted fields — full detail was completely unreachable. (e) Point 6b merged "Ritual Mingguan" and "Berbenah" into one tab (both were a weekly-check-in-and-write flow under two names) — explicit sign-off obtained by virtue of the user directly requesting it, satisfying `CLAUDE.md`'s "don't restructure Life Compass without asking again" note from the 2026-07-18 merge. (f) Point 6c (AI empathetic response after submitting a reflection) directly conflicted with `CLAUDE.md`'s existing "raw reflection text never reaches an LLM" rule — asked via `AskUserQuestion` before building anything; Rafli chose to allow a narrow, explicit exception (only the just-submitted single entry, never history, never routed through the shared `contextBuilder.js` pipeline that everything else's privacy guarantee still depends on). (g) Point 7 removed the Finance hero's redundant savings ring (superseded by the Tabungan fund card from the prior session), replaced with a top-3-spending-categories mini breakdown.
**Also mid-task**: the local dev server (not one this session started) went down partway through — started a replacement (`npm run dev`, backgrounded) to keep testing without interruption.
Full detail: `docs/features/finance-and-compass-rework.md`.

### 2026-07-19 — Finance: flat weekly budget (no categories) + Dana Darurat/Tabungan funds
Two rapid-fire follow-up requests, both landed same session as the Goals/Finance AI work below. (1) Rafli asked that every transaction automatically count toward a weekly limit, and that the limit be a single flexible number he sets — not per-category budgets ("makan, minuman, bensin... itu hapus saja"). Replaced the category-based `budgets` schema (shipped that same morning, itself already a rewrite of the original month-only version) with a flat `{month, week, limit}` row — no category dimension, every expense in the week counts automatically. Confirmed via `AskUserQuestion` that income should be shown for context but not count against the limit (a "budget" caps spending). (2) Immediately after, asked for Dana Darurat and Tabungan to be tracked as separate, first-class Finance entities with their own user-settable targets (Tabungan gets Rp10-juta-step milestones), explicitly separated from "pengeluaran," with the Goals module's savings-ladder goal looking up the Finance-side number instead of maintaining its own. Added `NON_SPENDING_CATEGORIES` (excluded from every spending aggregate — monthlyTotals, pie chart, weekly budget) and a `financeTargets` root-state object backing two new `FundCard`s on `/finance`. `goal-savings-ladder`'s milestones are now fully derived (`fundMilestones()`, every 10jt to target) instead of a hand-authored array — this made the old manual milestone-toggle checkbox meaningless (nothing consistent to toggle against a value that recomputes every render), so `toggleSavingsMilestone` was removed and the drawer shows milestones read-only.
**Verified the exclusion is real, not just cosmetic**: added a `saving`-category and an `emergency_fund`-category transaction, confirmed the Expense scorecard stayed at Rp0 for both, then added a normal `food` transaction and confirmed Expense correctly jumped to Rp50rb — proves the three touched selectors (`monthlyTotals`, `spendingByCategory`, `budgetWeeklyBreakdown`) all still work for genuine spending and only exclude the two savings categories.
Full detail: `docs/features/finance-funds-and-weekly-budget.md`.

### 2026-07-19 — Goals categorization + Smart Finance + AI action plans + AI financial planner
Four-point brief, all shipped in one pass. (1) Goals: `goalKind()` derives kuantitatif/kualitatif from existing `metric`/`contributions` presence — no migration, every seed goal already classifies correctly against the brief's own examples. Generalized the goal-savings-ladder's hardcoded "baseline + sum of saving-category transactions" formula into `linkedGoalCurrent()`, available to any goal via a new `linkedCategory` field — Goals↔Finance sync is no longer a one-off special case. (2) Finance: weekly budget got an optional per-week `weeklyOverrides` field (auto-proration remains the default, override wins when set) with a click-to-edit UI on the existing week/category accordion. Free-text transaction entry reuses `whatsapp/parser.js` **as-is** — it already did rule-based "beli kopi 4000 cash" → expense parsing for the paused WhatsApp feature; just exposed it as a manual "parse then review" field in Quick Add, zero new parsing logic. (3+4) Two new AI surfaces — Goals/Skills action-plan generator and a Finance financial planner — both **suggest-only**, confirmed via `AskUserQuestion` before building since `CLAUDE.md` had the `/ai` chat documented as strictly read-only and this was new territory: AI never writes, every "apply" action routes through an existing, already-trusted store action (`addCommitment`, `onUpdate({plan})`, `upsertBudget`). Extracted the daily AI rate limiter (previously inline in `/api/ai/chat`) into a shared module so all three AI routes draw from one budget; added a buffered (non-streaming) completion helper and a shared "extract JSON from a possibly-messy free-tier response" helper, both reused by the two new routes.
**Real bug caught during Playwright verification, not just eyeballing**: the new linked goal's progress *bar* used the live-synced value (`computeGoalProgress` → `linkedGoalCurrent`) but the card's *number display* still read the raw stored `metric.current` — created a goal, watched the bar say 5% while the text said "Rp 0", which don't agree. Fixed by routing the card's display through `linkedGoalCurrent()` too. General lesson: when a computed value has two render sites (a summary number and a bar/chart derived from the same thing), grep for every place the raw field is read directly and make sure they didn't miss the same fix.
Full detail: `docs/features/ai-action-plan-and-financial-planner.md`.

### 2026-07-19 — Cross-device sync (Supabase)
Rafli caught that data diverged between his laptop and phone and asked directly for Supabase sync — persistence was `localStorage`-only; the only existing Supabase usage was the paused WhatsApp webhook. Found Supabase already fully configured (real credentials in both `backend/.env` and `frontend/.env.local`) but `supabase/migrations/0001_initial_schema.sql`'s 11 relational tables are unusable from the browser: their RLS policies gate on `auth.uid()`, and this app has no login system, so `auth.uid()` is always null and every policy denies access. Presented Rafli a real trade-off via `AskUserQuestion` before building (per `CLAUDE.md`'s "explain plan, wait before architectural changes"): single JSONB state-blob table (small build, matches existing `persistableSlice()` shape) vs. full per-entity relational CRUD (correct long-term shape, multi-day build). He picked the blob, and picked "sync everything including private reflections" over carving those out (his reasoning: private means never shown to *other people*, not never leaves the device). Shipped `supabase/migrations/0003_app_state_sync.sql` (one row per user, RLS enabled with zero policies — service-role-only by construction, no auth needed), `frontend/src/app/api/sync/route.js` (GET pull / POST push via the service-role client), and reworked `hydrate()`/`persist()`/`reseed()` in `store.js` for local-first render + background reconcile + 800ms-debounced push, last-write-wins by timestamp. Verified with Playwright using two isolated browser contexts (no shared storage, i.e. genuinely simulating two devices) — add on "device A" reached Supabase and appeared on a brand-new "device B" context within ~1.5s; delete round-tripped the same way; confirmed clean via direct SQL after. Full detail: `docs/features/supabase-sync.md`.
**Process note**: applying the migration via `psql` with the DB password inline got blocked by the auto-mode safety classifier; asked Rafli directly rather than working around it (offered "paste into Supabase SQL editor" as the credential-free alternative) — he chose to approve the psql command as-is.

### 2026-07-19 — Career map: gamified single-track skill tree
Source: `docs/prompt/update-ui-career.md`. Replaced the dual-lane side-by-side career map (shipped 2026-07-18) with a segmented-control track toggle + a single winding "skill map" path per track — game-level-map aesthetic instead of a corporate timeline. `CareerTrail.jsx` now draws a smooth S-curve (per-segment cubic bezier) through nodes fixed at a 3-anchor zigzag; `TrailCard.jsx` renders 56px circular icon nodes (solid=completed, pulsing glow=in-progress "you are here", dashed hollow=planned "locked"). Title+date stay always-visible under the node; org/location/description/skills moved fully into the existing click-through drawer — deliberate decluttering.
**Real bug caught by Playwright, not just eyeballing a screenshot**: nodes were centered via a raw `style={{ transform: "translate(-50%,-50%)" }}` on a `motion.li`, which Framer Motion silently ignores (it owns `transform` for its own animations) — so every node rendered offset ~66px from its intended center, making the connecting path look like it skipped nodes entirely. First screenshot pass looked plausible at a glance; only measuring actual DOM bounding boxes + the rendered SVG `d` attribute exposed the real story (path math was correct all along — only the node's own CSS was broken). Fixed with Framer's native `x`/`y` style motion values instead of a transform string. **General lesson**: any future absolutely-positioned `motion.*` element needing a translate offset in this codebase must use `x`/`y`, never `transform` directly — and for spatial/layout bugs, verify with bounding boxes or attribute dumps, not just a screenshot glance.
Doc: SRS FR-CAREER-03 rewritten, addendum in `career-journey.md`. Verified: build clean, both tracks, drawer, mobile 390px, dark mode, no console errors introduced.

### 2026-07-19 — Budget hierarchical drill-down (Month → Week → Category)
Source: `docs/prompt/financial-hierarchy.md`. Refactored the flat budget-card grid on `/finance` into a 3-level accordion, reusing the exact expand/collapse pattern already established on `/guide` (button + `ChevronDown` + `AnimatePresence` height/opacity, reduced-motion aware). Key design call: **no schema change** — the `budgets` entity is still month+category only (§5.7 unchanged); weeks are 4 derived day-range buckets (`Math.ceil(daysInMonth/4)` per bucket) and each category's weekly "limit" is the monthly limit prorated by that bucket's day-count share. New pure selector `budgetWeeklyBreakdown(budgets, transactions, mk)` in `insights.js`. Month accordion defaults **open** (single month in view, no reason to hide it); weeks default collapsed. Delete now lives on the category row (any week) and calls `removeBudget(budgetId)` same as before. Verified via Playwright: 4 week rows render, expanding a week reveals 4 category rows with correct spent/allocated math and over-budget terracotta styling, delete buttons present, mobile 390px and dark mode both clean, production build passes. Doc: SRS FR-FIN-05 rewritten + traceability matrix row added.
**Lesson re-confirmed**: ran `npm run build` while `npm run dev` was still up (again) — clobbered `.next`, dev server served stale/404 chunks until restarted. Third time this has bitten a session; worth remembering unprompted next time rather than rediscovering via a failed Playwright run.

### 2026-07-19 — Fix: transactions silently failing to persist
Source: `docs/prompt/fix-crud-db.md` (reported as "transactions don't save to the database" — there is no traditional DB in this architecture, so the real question was why localStorage writes were being lost). Root cause found in `frontend/src/lib/store.js`: `saveToStorage()` wrapped `localStorage.setItem` in a try/catch that swallowed every error (comment: "quota exceeded — ignore"). `persist()`'s own try/catch never saw the failure, so `saveStatus` reported `"saved"` even when the write never happened — any add/delete looked instant in the UI but reverted on refresh (private-mode browsers throw `QuotaExceededError` on every `setItem`, which is the likely real-world trigger). Fix: removed the inner catch so the error propagates to `persist()`, which now also pushes a deduped `"save-failed"` warning notification (bell-icon badge, tone `warning`) alongside the existing topbar "Save failed" indicator — reused the existing notification system rather than adding a toast library. Guarded `hydrate()`'s first-visit branch (which also calls the now-throwing `saveToStorage`) by routing it through `persist()` instead of calling storage directly. No schema change, no new dependency. Verified: production build clean, dev server serves `/finance` 200 with no console errors.

### 2026-07-19 — Guideline page (`/guide`)
User-requested interactive tutorial section — the request itself was the explicit nav-change approval (new nav entry: Sidebar/Command Palette/TopBar, placed last after Asisten AI, BookOpen icon). Eight accordion sections (7 modules + cross-module shortcuts), each with numbered steps and **"coba langsung" buttons that trigger real app behavior** — `openQuickAdd()` with the section-appropriate type, `openPalette()`, navigation — rather than static screenshots/text. Per-section "Tandai dipahami" progress persisted to its own localStorage key (`guide-progress::v1`, deliberately outside the main store — it's UI preference, not life data). Content includes the newest features (goal evidence chip, ritual follow-up, correlation cards' ±3-week warm-up). Verified: 9 Playwright assertions (nav, accordion, Quick Add opens with correct tab preselected, palette opens, progress persists across reload, palette finds it via "panduan"), mobile/dark, build clean. Docs: PRD module table row 8, SRS FR-CROSS-07.

### 2026-07-18 — "Maksa growth" trio: friction visibility, ritual follow-up, goal evidence
Three functional briefs shipped (`friction-visibility.md`, `ritual-followup-prompt.md`, `goal-evidence-requirement.md`):
- **Engagement gap**: `engagementGapSignal(reviews, reflections)` → dashboard candidate at 2+ weeks lapse, `tone: "gentle-notice"` deliberately falls through to the neutral card branch (calm, not warning). Never-engaged accounts return null — you can't lapse what you never started. Doc: folded into `correlation-engine.md`.
- **Ritual follow-up**: schema verification found `nextWeekFocus` is a plain string[]; chose an *adjacent* additive map (`focusStatus: {index: status}`) over converting to object arrays — zero reader breakage. `unresolvedFocusItems()` (1–3 weeks back, current week never nags) + `setFocusResolution()` + a plain prompt card with resolve/carry/drop. Carry-forward fills the new form's first empty focus slot and marks the old item "carried". Doc: addendum in `life-compass.md`.
- **Goal evidence**: the brief assumed a `linkedTo` field might be needed — verification showed `goal.area` already provides linkage, so **zero schema change**. `goalEvidenceStatus()` (14-day window, per-area evidence from existing data only; growth/business exempt; young goals "pending" not "unproven"). Surfaces as a muted "belum ada bukti" chip + the "on track" stat now requires evidence. Finance evidence defined as "any transaction recorded in window" (engagement-shaped; budget-threshold-shaped evidence considered and deferred — decision documented in `goal-evidence.md`, new doc).
All verified via Playwright fixtures (20 assertions: lapsed/active/never-engaged; resolve/carry/drop/persistence/fresh-ritual-no-nag; unproven/exempt/pending/on-track-count), no console errors, production build clean.

### 2026-07-18 — Wave 3 trio: Career motion, Finance restraint, Life Compass reading/composing
Three independent presentation-only briefs shipped in one pass (`career-state-motion.md`, `finance-restraint.md`, `life-compass-reading-composing.md`):
- **Career**: state-tied motion — initial load now lands still (cards no longer all fade in); only session-added milestones animate (spring + dot-landing ring, prev-ids ref pattern); the drawer-triggering card holds a tone ring while the drawer is open. Dot-sizing motion skipped via the brief's own out-clause — and noted: `contributionToSize()` is actually dead code since the dual-track redesign (brief believed it was still used; kept per instructions). Addendum in `career-journey.md`.
- **Finance**: Stripe restraint — removed the redundant direction-icon circle from transaction rows (sign+color already encode it), amount promoted to the visually heaviest element (13.5px mono medium, tabular-nums), budget spent-figures to 15px, scorecards to p-5/24px, and **all row action buttons brought up to 44×44** (delete/toggle were ~26px — pre-existing a11y violation fixed as part of the guardrail). Add/delete transaction flows re-verified.
- **Life Compass**: two registers — Hero's Journey draft now a manuscript-style borderless field (left rule, serif 17px/1.8; same textarea+handlers); Timeline/drawer at 16px/1.8 reading rhythm with 62ch measure; letters at 17px/1.9, 58ch. Berbenah/Wins untouched per brief; minor register gap vs Berbenah noted in `life-compass.md`. Autosave 350ms verified unaffected.
All three: reduced-motion verified, no console errors, production build clean.

### 2026-07-18 — AI chat ghost-text / read-only reinforcement (Wave 3, UI/UX Elevation)
Source: `docs/prompt/ai-chat-ghost-text.md`. Presentation-only on `/ai`: assistant bubbles moved off `card` to a soft suggestion-register surface, dimmed 75% while streaming and settling on completion (reduced-motion aware, store flag + OS); markdown tables softened to hairlines (reported facts, not editable grid); starter prompts restyled as quoted serif-italic invitations with dashed borders; persistent lock-icon "read-only" line added under the input. Citation badge prominence unchanged; its Life Compass labels fixed to parenthesized form so the `·` separator doesn't split them into fake extra modules. Verified with a **mocked SSE route** in Playwright (deterministic, no real OpenRouter calls) — a pattern worth reusing for future `/ai` UI tests. `contextBuilder`/`promptBuilder`/`openrouter` untouched. Addendum in `docs/features/ai-assistant.md`.

### 2026-07-18 — Command Palette + Quick Add treatment (Wave 2, UI/UX Elevation)
Source: `docs/prompt/command-pallete-quick-add.md`. Presentation-only on the two highest-frequency surfaces: new `.surface-glass` utility in globals.css (bg-elev at 86% opacity + backdrop-blur 18px, existing tokens only — 86% keeps text contrast effectively unchanged, AA-safe) applied to both panels; palette gained a 120ms fade/scale entrance and rows got min-height 44px (was ~40px — small a11y win) with a 100ms transition-colors selection cue; Quick Add's existing spring entrance made reduced-motion-aware (it previously animated unconditionally). Reduced motion honored via store flag + OS `useReducedMotion()`, same pattern as Wave 1. Playwright re-verified the "must never break" keyboard flows: Ctrl+K open, arrows/filter/Enter navigate, Esc closes palette; Tab/Shift-Tab in Quick Add. **Functional gap noticed, deliberately not fixed (per brief's "note, don't fix")**: Esc does not close the Quick Add modal — no keydown handler exists; recorded in SRS FR-CROSS-02. Docs: SRS FR-CROSS-01/02 notes; no standalone feature doc.

### 2026-07-18 — Dashboard insight-card visual treatment (Wave 1, UI/UX Elevation)
Source: `docs/prompt/dashboard-insight-cards.md`. Presentation-only: correlation card numbers moved from `body` into a new `meta` field (muted mono line, Posthog annotation pattern); "Insight lanjutan" cards switched from tone-tinted backgrounds to neutral cards with 3px left-border accents (calm severity — terracotta/forest/lime, no new hues); dashboard stagger entrance switched to spring easing and extended to the lanjutan grid. Notable fix along the way: the dashboard previously ignored reduced-motion entirely — now honors both the store flag and OS `prefers-reduced-motion` (Framer `useReducedMotion()`), since the global CSS kill-switch can't stop JS-driven springs. Verified both paths in Playwright. Lesson recorded: never run `npm run build` while the dev server is running — it clobbers `.next` and the dev server serves 404 chunks until restarted. Addendum in `docs/features/correlation-engine.md`; no standalone feature doc (per brief).

### 2026-07-18 — Cross-Signal Correlation Engine
Source: `docs/prompt/add-correlation-engine.md` (highest-priority idea from the "what makes this app powerful" brainstorm; Life Score and Retrospective/Chapter View briefs will consume its output later). Three purpose-built pure selectors in `insights.js` — deliberately *not* a generic correlation framework: `stressSpendingCorrelation`, `energyGoalVelocityCorrelation`, `milestoneEnergyCorrelation`. Each returns `null` below its data threshold (momentumIndex honesty pattern); non-null results append to `buildInsights()`'s candidate pool (`buildInsights` gained an optional `reviews` param). One deliberate spec addition: a `tone` field on the return shape, since card styling is tone-driven and direction is only known inside each function. Design note: `goal.progress` has no history — goal "velocity" is measured via goal-milestone `achievedAt` timestamps, the only timestamped goal-progress signal that exists. With current seed data all three return null (rituals lack energy/stress values) — cards appear as real ritual data accumulates. Full detail: `docs/features/correlation-engine.md`.

### 2026-07-18 — Docs sync (CLAUDE.md, PRD.md, SRS.md, ai-assistant.md)
PRD.md → v2.1, SRS.md → v1.1. Reconciled both specs with the Life Compass merge and Career Journey redesign below (module lists, FR-REFLECT+FR-REVIEW → FR-COMPASS, FR-CAREER-03/04 rewritten, data dictionaries updated, traceability matrix repointed). Created `docs/features/life-compass.md` and `docs/features/career-journey.md` (implementation-detail docs, same pattern as the existing `ai-assistant.md`/`whatsapp-integration.md`). This file and `docs/CLAUDE_PROJECT_INSTRUCTIONS.md` were created in the same pass, for the claude.ai Project setup. Committed by Rafli as `3ff01c2`.

### 2026-07-18 — Dev server "feels stuck" navigation fix
Root cause: Next.js dev server's `onDemandEntries` default (5-page buffer, 60s idle disposal) — with ~9 routes in this app, navigating between sections evicted and recompiled routes repeatedly, each recompile costing 2–4s and feeling like a freeze. Fixed in `frontend/next.config.js` (`onDemandEntries: { maxInactiveAge: 1hr, pagesBufferLength: 15 }`, dev-only, no effect on production builds). Measured before/after with Playwright: before, revisiting an evicted route cost 2.4–3.7s indefinitely through a session; after, every route compiles once then stays sub-second (0.13–0.35s) for the rest of the session. First-visit-per-session compile cost is unavoidable in dev mode — for zero compile lag even on first visit, the real fix is running `npm run build && npm run start` instead of `npm run dev`.

### 2026-07-18 — Career Journey dual-track redesign
Source: `docs/prompt/revamp-career-journey.md`. Old map was icon-only nodes on a single winding path (title/date hidden behind a click, jobs mixed with certs/education). Replaced with two parallel lanes ("Jejak Profesional" / "Milestone & Pencapaian") of text-visible cards (`CareerTrail.jsx` + new `TrailCard.jsx`, replacing `TrailNode.jsx`). Schema gained `track`, `endMonth`/`endYear`/`ongoing` (date ranges + "Sekarang"), `location`, `highlights[]`. Seed data replaced entirely with 13 real entries parsed from Rafli's resume (`Profile (6).pdf`, uploaded to project root) — 6 jobs tracing the administration→data-analytics pivot, 2 education, 5 certifications. **Caveat still open**: the resume listed no dates for the 5 certifications — their month/year are estimated against the career timeline (e.g. Google Advanced Data Analytics ≈ mid-2025) and should be corrected via the milestone detail drawer if Rafli has the real dates. Full detail: `docs/features/career-journey.md`.

### 2026-07-18 — Life Compass (merged Reflection + Weekly Review)
Source: `docs/prompt/merge-weekly-reflection.md`. Nav-structure change explicitly approved (overriding `CLAUDE.md`'s default "don't change nav" rule) via an `AskUserQuestion` checkpoint before implementing — that's the pattern to repeat for any future nav-affecting request, not a one-time exception. Merged at the module/nav/UI level only — `reflections` and `reviews` stayed separate Zustand arrays (different cadences: ad-hoc journaling vs. weekly synthesis); rewriting Reflection's CRUD/timeline/templates wasn't in scope. Shipped: Ritual Mingguan tab (mood/energy/stress, rule-based "Hero's Journey" draft paragraph — deliberately *not* a new LLM-generation surface, consistent with "insights stay rule-based"), Momentum vs Burnout indicator, Butterfly Effect card (links most-reflected goal to its live progress %). **Found and fixed a real privacy bug in the same pass**: `buildReview()` in `contextBuilder.js` was sending raw weekly-review text into the AI assistant's context unfiltered (Weekly Review never had an `isPrivate` field, so the existing string-match privacy filter couldn't catch it) — now aggregated via `reviewInsights()` like Reflection already was. Full detail: `docs/features/life-compass.md`.

## Open loose ends / known caveats

- Career milestone certification dates (5 entries) are estimated, not resume-confirmed — see above.
- WhatsApp → Finance quick-add is paused (Phase 2), code/migrations already built and tested, just waiting on a dedicated WhatsApp Business number — see `docs/features/whatsapp-integration.md`. Don't resume without being asked.
- Cross-device sync is last-write-wins with no real-time push between open tabs — see `docs/features/supabase-sync.md` "Known limitations." Fine for one person on one device at a time; revisit if concurrent-device editing ever becomes a real pattern.

## Reference index

| Doc | What it's for |
|---|---|
| `CLAUDE.md` | Binding project rules — coding standards, workflow, design system, what never to do |
| `docs/PRD.md` | Product requirements — user stories, feature modules, roadmap |
| `docs/SRS.md` | Technical spec — functional/non-functional requirements, data dictionary, validation rules |
| `docs/features/*.md` | Status + architecture-decision docs for individually-shipped features (AI assistant, Life Compass, Career Journey, WhatsApp, Supabase sync, Goals/Finance AI upgrade, Finance funds/weekly budget, Finance corrections + Compass rework) |
| `docs/prompt/*.md` | Original task briefs handed to Claude Code for major features — useful as examples of the hand-off format when this Project drafts a new one |
