# Client-exposed — safe to expose (already client-side today).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — never prefix NEXT_PUBLIC_, never import into "use client" files.
SUPABASE_SERVICE_ROLE_KEY=

# WhatsApp Cloud API (Meta) — used by /api/whatsapp webhook.
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_APP_SECRET=
WHATSAPP_ALLOWED_NUMBER=
