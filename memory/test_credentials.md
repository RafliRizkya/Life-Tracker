# Test Credentials

Aplikasi berjalan dengan **single seeded user tanpa login screen** (Phase 1).
Tidak ada halaman auth yang perlu diuji.

- Seed user id: `rafli-akbar`
- Full name: `Rafli Akbar`
- Semua data persisten via `localStorage` (key: `rafli-life-tracker::state::v1`).

Supabase env kosong secara default — aplikasi tetap fungsional penuh via
local store.
